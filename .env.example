# Postgres connection string (Supabase project "testco1").
# Get the real value from the Supabase dashboard: Project Settings > Database
# > Connection string (use the "Connection pooler" URI for Render).
DATABASE_URL=
